# TTP Residency Assignments 03 and 04

(DOM Challenge I is in its separate directory for clarity, DOM Challenge II .html, .css, and .js files are in the `root` directory for the purpose of GitHub Pages deployment).
